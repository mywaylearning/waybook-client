machine:
  node:
    version: 4

dependencies:
  pre:
    - sudo pip install closure-linter awscli;

deployment:
  staging:
    branch: master
    commands:
      - npm run deploy:staging
  wayme:
    branch: wayme
    commands:
      - npm run deploy:production
