machine:
  node:
    version: 4

dependencies:
  pre:
    - sudo pip install closure-linter awscli

test:
  pre:
    - bower install

deployment:
  staging:
    branch: master
    commands:
      - npm run deploy:staging
